{
  "name": "inferno",
  "version": "1.0.0",
  "private": true,
  "description": "Inferno — hypercube: zivo telo od koda (web) + Android launcher (Capacitor).",
  "scripts": {
    "cap:sync": "cap sync android",
    "android:debug": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "web-push": "^3.6.7",
    "@capacitor/core": "^6.2.0",
    "@capacitor/android": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
